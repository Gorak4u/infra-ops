---
# Hiera Data for test
cassandra::cluster_name: "test"
cassandra::seeds: []

# Backup Configuration
backup::schedule:
  full: "0 0 * * 0"
  incremental: "0 * * * *"
  retention: 7

monitoring::enabled: true
