---
# Hiera Data for prod
cassandra::cluster_name: "prod"
cassandra::seeds: []

# Backup Configuration
backup::schedule:
  full: "0 0 * * 0"
  incremental: "0 * * * *"
  retention: 7

monitoring::enabled: true
